import React from "react";

interface ImportAccountsDialogProps {
  open: boolean;
  onOpenChange: (open: boolean) => void;
  onImport: (accounts: any[]) => void;
}

export default function ImportAccountsDialog({ open, onOpenChange, onImport }: ImportAccountsDialogProps) {
  // TODO: Implement import accounts dialog
  return null;
}
